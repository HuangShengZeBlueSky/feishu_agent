# 资源目录说明

更新时间：2026-04-28 11:10

## 当前规则

业务主路径不再读取 `resources/mock`。真实运行依赖：

- `resources/config/本地飞书密钥.env`：飞书应用、多维表格 token/table id，本地忽略，不入库。
- `resources/config/本地模型secret.env`：LLM 网关配置，本地忽略，不入库。
- `resources/config/本地模型资源secret.json`：模型资源清单，本地忽略，不入库。
- 真实飞书 Base：当前事实库，包含上下文来源、行动项、决策、风险。

## 已删除的旧资源

`resources/mock` 样例数据已经删除。旧 mock CLI 入口也已经从主程序移除。
