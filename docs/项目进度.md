# 项目进度

更新时间：2026-04-25

## 当前阶段

正在按最新版 `spec.md` 执行：先用 mock 模式跑通 6 个例子，再逐步切到 feishu 模式。

## 已完成

- OpenClaw CLI 已安装，但 Windows 原生 Feishu plugin/Gateway 不稳定，MVP 暂时直接走飞书 OpenAPI。
- 新 App ID / App Secret 已写入本地忽略密钥文件；飞书 `tenant_access_token` 和 `bot.info` 获取验证通过。
- 多维表格创建 app/table/record/update 能力验证通过。
- 已实现本地会后行动项闭环：会议文本 -> 行动项/决策/风险 -> JSON/摘要 -> 多维表格 dry-run。
- 已建立统一依赖清单：缺什么、从哪里来、谁提供、怎么验证、卡住什么。
- 已把 mock 资源集中到 `resources/mock/`。
- 已建立资源目录说明：[资源目录说明](资源目录说明.md)。
- 已把新 App ID 写入模板；App Secret 只放本地忽略文件 `resources/config/本地飞书密钥.env`。
- 已跑通 6 个 mock 例子：上下文清单、会后闭环、会前卡片、推进表对账、风险周报、角色约会、文档预览。
- 已实现 `run_log`：每次运行记录输入、输出、写入计划、实际写入、错误。

## 正在做

- 等待真实飞书对象 ID：多维表格 app token/table id、chat_id、doc_id、minute_token、task_guid、calendar_id。

## 下一步

1. 让你审查 `run_logs/` 和 `output/` 里的黑盒结果。
2. 配齐多维表格 app token/table id 后，切换真实多维表格写入。
3. 再接群消息、会议妙记、任务、文档、日历。

## 当前缺口

详见：[飞书依赖统一清单](飞书依赖统一清单.md)。

## 最近验证命令

```powershell
python -m src.cli run-all-mock
python tools\feishu_capability_probe.py --json
```

结果：

- `run-all-mock` 通过，生成 `output/六个例子总览.json` 和多份 `run_logs/*.json`。
- 飞书只读探针中 `tenant_access_token`、`bot.info` 通过。
- 日历仍缺 `calendar:calendar:readonly` / `calendar:calendar:read` 等权限。
- 群聊、文档、任务、会议妙记、多维表格目标表读取仍缺真实对象 ID。
