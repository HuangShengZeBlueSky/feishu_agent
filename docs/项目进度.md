# 项目进度

更新时间：2026-04-25 23:59

## 当前阶段

项目已经从纯 mock 验证推进到两条真实链路：

1. 飞书 OpenAPI：App 鉴权、多维表格创建和写入已通过。
2. OpenClaw + 飞书机器人：OpenClaw Gateway 已运行，Feishu channel 已连接，能力探针已通过。

## 已完成

- 飞书 App ID / App Secret 已写入本地忽略文件：`resources/config/本地飞书密钥.env`。密钥不写入文档和源码。
- `tenant_access_token` 和 `bot.info` 已验证通过，说明 App 凭证可用。
- `lark-cli` 已安装并初始化到当前 App；当前是 bot 身份，未做用户 OAuth 登录。
- 已用机器人创建真实飞书多维表格：项目上下文清单、行动项、决策、风险 4 张表。
- 已真实写入多维表格：行动项 3 条、决策 1 条、风险 1 条、上下文来源 6 条。
- mock 资源已统一到 `resources/mock/`，配置模板统一到 `resources/config/`。
- 已实现多智能体会议模拟，角色包括项目经理、产品、后端、前端、测试。
- OpenClaw 已安装到 ASCII 路径：`C:\openclaw-npm`，规避中文用户名路径下插件依赖安装失败。
- OpenClaw 配置已修复：工作区改为 `C:\openclaw-workspace`，Feishu 账号名为“小龙虾项目上下文管家”。
- OpenClaw Gateway 当前运行在 `127.0.0.1:18789`，健康检查通过：`Gateway Health OK`，`Feishu: ok`。
- OpenClaw Feishu channel 已验证：`enabled, configured, running`，能力探针 `Probe: ok`。
- OpenClaw Feishu 支持能力已列出：direct/channel 消息、发送、广播、读取、编辑、回复、线程回复、置顶、成员信息、频道信息、反应等。
- 已把 OpenClaw 默认模型切到火山 Ark：`volcengine/ep-20260423222827-6lcn6`，实测实际模型为 `doubao-seed-2-0-pro-260215`。
- 已保存本地模型资源清单到忽略文件：`resources/config/本地模型资源secret.json`。
- 已从 Gateway 日志捕获真实飞书群消息，测试群 ID 为 `oc_17a66849f49461ef2b6f42f89e0f48f4`。
- 已把该测试群加入 OpenClaw 飞书 `groupAllowFrom` 白名单。
- 已定位 `lark-cli` 可执行文件：`C:\Users\黄胜泽\AppData\Roaming\npm\lark-cli.cmd`。当前 PowerShell PATH 未自动识别 `lark-cli`，脚本先使用完整路径。
- 已新增一键验收命令：`python tools\验收小龙虾能力.py`，最新结果为 15/15 通过。

## 最近验证命令

```powershell
python -m src.cli run-all-mock
python tools\初始化飞书多维表格.py
python -m src.cli post-meeting-local --minutes-file resources\mock\sample_minutes.txt --write-bitable
python tools\同步上下文清单到飞书.py
python -m src.cli simulate-agents-meeting --project-id pay_project --meeting-title 支付项目评审会

& 'C:\openclaw-npm\openclaw.cmd' config validate
& 'C:\openclaw-npm\openclaw.cmd' gateway health
& 'C:\openclaw-npm\openclaw.cmd' gateway status
& 'C:\openclaw-npm\openclaw.cmd' channels status --deep
& 'C:\openclaw-npm\openclaw.cmd' channels capabilities --channel feishu
& 'C:\openclaw-npm\openclaw.cmd' models status --plain
python tools\验收小龙虾能力.py
```

## 当前结论

OpenClaw 必须项已经装好并接上飞书机器人。当前不是飞书凭证问题，也不是 Feishu plugin 缺失问题；最新验证显示 Gateway 可达、Feishu main 账号运行、Feishu 能力探针通过。真实群消息已经到达过 Gateway，之前被群白名单拦截；现在测试群已加入白名单，需要你再次在群里 @ 小龙虾验证真实回复。

需要注意：OpenClaw 启动后会做模型价格、插件依赖、技能扫描和 agent 回复准备，短时间内健康探针可能超时。等待初始化完成后，`gateway health` 可恢复为 `OK`。

## 下一步

1. 让你在飞书里直接给“小龙虾项目上下文管家”发一条测试消息，验证真实入站消息和 OpenClaw 回复。
2. 在测试群 `oc_17a66849f49461ef2b6f42f89e0f48f4` 里发送：`@小龙虾项目上下文管家 请只回复：验收收到`。
3. 提供真实文档、会议妙记、任务、日历对象，继续验证读取能力。
4. 把通过的真实对象写回多维表格，形成项目上下文总表。

## 仍缺依赖

详见 [飞书依赖统一清单](飞书依赖统一清单.md)。简要说，当前最缺的是：真实群回复确认、真实文档 ID、会议妙记 token、任务 GUID、日历 ID 或用户授权。
